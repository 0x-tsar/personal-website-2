import React from "react";
import styled from "styled-components";

export const Container = styled.div`
  background-color: blue;
  color: white;
  font-size: 2em;
`;

type Props = {};

export const PanelArticles = (props: Props) => {
  return <Container>PanelArticles</Container>;
};
